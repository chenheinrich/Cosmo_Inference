name: Python package
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.7]
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v1
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          sudo apt-get install -y \
              openmpi-bin \
              openmpi-common \
              libopenmpi-dev \
              libopenblas-dev \
              liblapack-dev \
              gfortran \
              git 
          python -m pip install -e ./src/lss_theory/
          python -m pip install -e ./src/spherex_cobaya/
          python -m pip install pytest
          python -m pip install pytest-cov
      #- name: Run tests
        #run: |
        #  python -m pytest ./tests/ -v -m short
        #  python -m pytest ./theory/tests/ -v
